# MobileAudit v1.0 — Dependências Python
# Instale com: pip install -r requirements.txt

# Interface CLI rica (cores, tabelas, progress)
rich>=13.0.0

# Suporte completo a iOS (opcional mas recomendado)
# Permite acesso mais profundo que o libimobiledevice CLI
pymobiledevice3>=4.0.0

# Formatação de tabelas no terminal
tabulate>=0.9.0

# Geração de relatórios HTML
# (builtin: json, datetime, os, re, subprocess, shutil)
